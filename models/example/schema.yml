
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: dim_top_trainees
      description: "select top trainees based on business understanding competency score"
      columns:
          - name: fullname
            description: "Unique Identifier for each trainee"
            tests:
                - unique
                - not_null